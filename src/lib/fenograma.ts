// TEMPORARY_FACADE: keep public exports stable while Fenograma domain logic
// is split into smaller files. Add new implementation in dedicated files,
// not in this facade.

export type * from "@/lib/fenograma-types";
